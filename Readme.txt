This is a django application so for starting the server the command is "python3 manage.py runserver"
Before that we need to make "python3 manage.py makemigrations" and "python3 manage.py migrate" to ensure that DB tables are up and running.
